<?xml version="1.0" encoding="utf-8" ?>
<!DOCTYPE properties SYSTEM "http://java.sun.com/dtd/properties.dtd">
<properties>

    <entry key="rentalable">
        UPDATE TBL_STATUS
        SET STATUS_RENT = ?, STATUS_RESERVE = ? , DATE_RENT = ?
        WHERE ISBN = ?
    </entry>
    <entry key="returnBook">
        UPDATE TBL_STATUS
        SET STATUS_RENT = ?, STATUS_RESERVE = DEFAULT, DATE_RENT = NULL
        WHERE ISBN = ?

    </entry>
    <entry key="titleSearch">

        SELECT
        A.* , B.STATUS_RENT, B.STATUS_RESERVE
        FROM
        TBL_BOOK A JOIN TBL_STATUS B
        ON
        A.ISBN = B.ISBN
        WHERE
        A.SUBJECT = ?
    </entry>
    <entry key="allSearch">
        SELECT
        A.* , B.STATUS_RENT, B.STATUS_RESERVE
        FROM
        TBL_BOOK A JOIN TBL_STATUS B
        ON
        A.ISBN = B.ISBN
    </entry>

    <entry key="loginCheck">
        SELECT  user_code
        FROM    tbl_user
        WHERE   user_id = ? AND
                user_pwd = ?
    </entry>

    <entry key="signUp">
        INSERT into tbl_user(
            user_code,
            name,
            phone,
            user_id,
            user_pwd
        )VALUES (
            NULL,
            ?,
            ?,
            ?,
            ?
        )
    </entry>


</properties>